version: '3.8'

services:
  valkyrie-bot:
    build: .
    container_name: valkyrie_quant_desk
    restart: always
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=5000
    volumes:
      - ./trade_history.json:/app/trade_history.json
